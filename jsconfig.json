{
  "compilerOptions": {
    "baseUrl": ".",  // Base directory for non-relative module imports
    "paths": {
      "components/*": ["components/*"],  // Alias for the components directory
      "utils/*": ["utils/*"],  // Alias for the utils directory
      "styles/*": ["styles/*"]  // Alias for the styles directory
    }
  }
}